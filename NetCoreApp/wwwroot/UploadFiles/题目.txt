1、传入企业类型，获取所有该类型的企业，返回企业列表。

2、传入省份编码，获取该省份所有的地区信息，返回地区树状列表。

3、种子种苗-采收管理环节，获取采收环节所有产量，返回总产量。

4、饮片生产-销售出库环节，传入产品批号，获取该条数据所属企业的企业名称，返回企业名称。

5、药材种植-农户档案环节，传入企业Id、pageSize、pageNumber，获取指定企业所有农户信息，并按照面积降序排序，返回农户分页数据。